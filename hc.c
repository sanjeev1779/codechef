#include<stdio.h>
struct node
{
    int data;
    node *next;
};

main()
{
        int i;
        int n;
        scanf("%d",&n);
        while(n--)
        {
            node *temp= (node*)malloc(sizeof(node));
            temp
        }

}
