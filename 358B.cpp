#include<iostream>
#include<vector>
#include<string.h>
#include<stdio.h>
#include<maap>
#include<math.h>
#include<algorithm>
#define LL long long
#define P(N) printf("%d\n",N);
#define S(N) scanf("%d",&N);
#define pb push_back
#define mp make_pair
using namespace std;
main()
{
    int n;
    S(n);
    string crypt;
    string str[n+2];
    for(i=0;i<n;i++)
    {
        cin>>str[i];
    }
    cin>>crypt;
    int l= crypt.size();
    for(i=0;i<l;i++)
    {
        if(crypt[i]=='<')
        {
            flag=1;
        }
        if(crypt[i])
    }
return 0;
}
