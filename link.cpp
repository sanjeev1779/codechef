#include<string.h>
#include<stdio.h>
#include<math.h>
#define LL long long
#define P(N) printf("%d\n",N);
#define S(N) scanf("%d",&N);

struct node *node
{
    int data;
    struct node *next;
};
main()
{

    int n;
    scanf("%d",&n);
    while(n--)
    {
        temp= struct node *
    }

return 0;
}
