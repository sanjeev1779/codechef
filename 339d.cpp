#include<iostream>
#include<vector>
#include<string.h>
#include<stdio.h>
#include<map>
#include<math.h>
#include<algorithm>
#define LL long long
#define P(N) printf("%d\n",N);
#define S(N) scanf("%d",&N);
#define pb push_back
using namespace std;
main()
{
    LL n,m;
    cin>>n>>m;
    int p,b;
    LL l= pow(2,n);
    LL a[l+2];
    for(i=1;i<=l;i++)
        scanf("%lld",&a[i]);
    while(m--)
    {
        S(p);
        S(b);
        a[p]=b;
        for(i)
    }

return 0;
}
